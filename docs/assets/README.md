# Documentation assets

Store files referenced from the docs here:

- Screenshots (`screenshots/`)
- Diagrams and flowcharts (`diagrams/`)
- Exported samples (`samples/`)

Use relative links from feature docs, for example:

```markdown
![POS checkout flow](assets/screenshots/pos-checkout.png)
```

Keep filenames lowercase with hyphens. Prefer PNG or SVG for UI captures.
