[pytest]
DJANGO_SETTINGS_MODULE = config.settings.development
pythonpath = ../popy-be
testpaths = backend
python_files = test_*.py
addopts = -v --tb=short --strict-markers
markers =
    smoke: live server checks (optional, server must be running)
